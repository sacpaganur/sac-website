const puppeteer = require('puppeteer');(async () => {const browser = await puppeteer.launch();const page = await browser.newPage();page.on('console', msg => console.log('PAGE LOG:', msg.text()));await page.goto('https://stacpaganur.web.app', {waitUntil: 'networkidle2'});await new Promise(r => setTimeout(r, 8000));const text = await page.$eval('#public-visitor-number', el => el.innerText);console.log('VISITOR COUNT IS:', text);await browser.close();})();
