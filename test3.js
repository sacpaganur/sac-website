const puppeteer = require('puppeteer');(async () => {const browser = await puppeteer.launch();const page = await browser.newPage();await page.goto('https://stacpaganur.web.app', {waitUntil: 'networkidle2'});const isWinDef = await page.evaluate(() => typeof window.SAC_DATABASE !== 'undefined');const isDef = await page.evaluate(() => typeof SAC_DATABASE !== 'undefined');console.log('window.SAC_DATABASE:', isWinDef);console.log('SAC_DATABASE:', isDef);await browser.close();})();
